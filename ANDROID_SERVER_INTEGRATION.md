# 🚀 Android-Server Integration Guide

## 📋 Обзор интеграции

Android приложение VkBookAndroid успешно интегрировано с сервером VkBookServer. Все основные компоненты настроены и готовы к использованию.

## 🔧 Что было реализовано

### ✅ Завершенные задачи:
- [x] **Базовая структура проекта** - Retrofit, OkHttp, Gson настроены
- [x] **Модели данных** - ArmatureMarker, ArmatureCoordsData, ApiResponse и др.
- [x] **API интерфейс** - Полный набор endpoints для работы с сервером
- [x] **Сетевая конфигурация** - Настроены таймауты, логирование, CORS
- [x] **Репозиторий** - ArmatureRepository с методами для всех операций
- [x] **UI компоненты** - Кнопки синхронизации и тестирования подключения
- [x] **Управление файлами** - Загрузка/скачивание PDF, Excel, JSON файлов
- [x] **Тестирование** - ServerConnectionTest для проверки интеграции

## 🌐 Настройка подключения

### URL сервера:
```kotlin
// В NetworkModule.kt
private const val BASE_URL = "http://10.0.2.2:8082/" // Для эмулятора
// private const val BASE_URL = "http://192.168.1.100:8082/" // Для реального устройства
```

### Доступные endpoints:
- `GET /actuator/health` - Проверка состояния сервера
- `GET /api/files/pdf` - Список PDF файлов
- `GET /api/files/excel` - Список Excel файлов
- `GET /api/files/json/armature_coords.json` - Координаты арматур
- `GET /api/files/pdf/{filename}` - Скачивание PDF
- `POST /api/files/pdf/upload` - Загрузка PDF
- И другие...

## 🎯 Как использовать

### 1. Тестирование подключения
- Нажмите кнопку **"🔍 Тест"** в верхней панели
- Проверьте логи в Android Studio (фильтр: "ServerConnectionTest")
- Увидите результат в Toast сообщении

### 2. Синхронизация данных
- Нажмите кнопку **"🔄 Синхронизация"**
- Приложение загрузит:
  - armature_coords.json
  - PDF схемы
  - Excel файлы с данными
- Статус отображается в текстовом поле

### 3. Работа с данными
- Данные автоматически загружаются во фрагменты
- Поддерживается поиск и фильтрация
- Маркеры арматур отображаются на схемах

## 📱 Основные компоненты

### ArmatureRepository
```kotlin
// Проверка подключения
suspend fun checkServerHealth(): Boolean

// Загрузка данных
suspend fun loadArmatureCoordsFromServer(): ArmatureCoordsData?
suspend fun getPdfFilesFromServer(): List<String>
suspend fun getExcelFilesFromServer(): List<String>

// Скачивание файлов
suspend fun downloadPdfFile(filename: String): ResponseBody?
suspend fun downloadExcelFile(filename: String): ResponseBody?

// Загрузка файлов
suspend fun uploadPdfFile(file: MultipartBody.Part): String?
suspend fun uploadExcelFile(file: MultipartBody.Part): ApiResponse<FileInfoDto>?
```

### SyncService
```kotlin
// Полная синхронизация
suspend fun syncAll(): SyncResult

// Отдельные операции
suspend fun syncArmatureCoords(): Boolean
suspend fun syncPdfFiles(): Boolean
suspend fun syncExcelFiles(): Boolean
```

### ServerConnectionTest
```kotlin
// Быстрый тест
suspend fun quickConnectionTest(): Boolean

// Полный тест всех endpoints
fun runFullTest()
```

## 🔍 Отладка

### Логи для мониторинга:
- `NetworkModule` - HTTP запросы и ответы
- `SyncService` - Процесс синхронизации
- `ServerConnectionTest` - Результаты тестирования
- `ArmatureRepository` - Операции с данными

### Типичные проблемы:
1. **Сервер недоступен** - Проверьте URL и порт
2. **Таймауты** - Увеличьте значения в NetworkModule
3. **CORS ошибки** - Сервер настроен на `*`, должно работать
4. **Файлы не загружаются** - Проверьте права доступа к файловой системе

## 🚀 Следующие шаги

### Возможные улучшения:
- [ ] Добавить аутентификацию
- [ ] Реализовать кэширование
- [ ] Добавить офлайн режим
- [ ] Улучшить обработку ошибок
- [ ] Добавить прогресс-бары для загрузки

### Настройка для продакшена:
- [ ] Использовать HTTPS
- [ ] Настроить сертификаты
- [ ] Добавить retry логику
- [ ] Оптимизировать размеры файлов

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в Android Studio
2. Убедитесь, что сервер запущен на порту 8082
3. Проверьте сетевое подключение
4. Используйте кнопку "🔍 Тест" для диагностики

---

**Интеграция готова к использованию! 🎉**
