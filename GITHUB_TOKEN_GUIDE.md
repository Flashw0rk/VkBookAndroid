# 🔐 Пошаговая инструкция: Получение Personal Access Token (PAT) на GitHub

## 📋 **ШАГ 1: Войти в GitHub**

1. Откройте браузер и перейдите на [https://github.com](https://github.com)
2. Войдите в свой аккаунт (если не залогинены)

---

## 📋 **ШАГ 2: Открыть настройки токенов**

1. Нажмите на **аватар** в правом верхнем углу
2. Выберите **Settings** (Настройки)
3. В левом меню прокрутите вниз до раздела **Developer settings**
4. Нажмите на **Developer settings**

---

## 📋 **ШАГ 3: Создать новый токен**

1. В левом меню выберите **Personal access tokens**
2. Выберите **Tokens (classic)** (или **Fine-grained tokens** для более детального контроля)
3. Нажмите кнопку **Generate new token** (или **Generate new token (classic)**)

---

## 📋 **ШАГ 4: Настроить токен**

### Для **Tokens (classic)**:

1. **Note** (Название): Введите описание, например: `VkBookAndroid Push Token`
2. **Expiration** (Срок действия): 
   - Выберите срок (например, 90 дней, 1 год или **No expiration** для постоянного токена)
   - ⚠️ **Внимание**: Для постоянного токена выберите "No expiration"
3. **Select scopes** (Выберите права):
   - ✅ Обязательно отметьте **`repo`** (полный доступ к репозиториям)
     - Это включает: `repo:status`, `repo_deployment`, `public_repo`, `repo:invite`, `security_events`
   - ✅ Можно также отметить **`workflow`** (если используете GitHub Actions)
4. Прокрутите вниз и нажмите **Generate token**

### Для **Fine-grained tokens** (более безопасный вариант):

1. **Token name**: Введите название, например: `VkBookAndroid Push Token`
2. **Description**: Описание (опционально)
3. **Expiration**: Выберите срок действия
4. **Repository access**:
   - Выберите **Only select repositories**
   - Выберите репозиторий **VkBookAndroid**
5. **Repository permissions**:
   - **Contents**: Выберите **Read and write**
   - **Metadata**: Выберите **Read-only** (устанавливается автоматически)
6. Нажмите **Generate token**

---

## 📋 **ШАГ 5: Скопировать токен**

⚠️ **КРИТИЧНО**: Токен показывается **ТОЛЬКО ОДИН РАЗ**!

1. После создания токена вы увидите его на экране
2. Токен будет выглядеть примерно так: `ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`
3. **НЕМЕДЛЕННО** скопируйте токен:
   - Нажмите на иконку копирования рядом с токеном
   - Или выделите весь токен и скопируйте (Ctrl+C)
4. **Сохраните токен в безопасном месте** (например, в менеджере паролей)

---

## 📋 **ШАГ 6: Настроить Git для использования токена**

### Вариант 1: Использовать токен в URL (быстро, но менее безопасно)

```bash
cd "D:\Java\Project RU VK-50\Projects Android\VkBookAndroid"
git remote set-url origin https://ghp_ВАШ_ТОКЕН@github.com/Flashw0rk/VkBookAndroid.git
```

**Замените `ВАШ_ТОКЕН` на скопированный токен!**

### Вариант 2: Использовать Git Credential Manager (рекомендуется)

1. При следующем `git push` Git запросит учетные данные
2. **Username**: Ваш GitHub username (например, `Flashw0rk`)
3. **Password**: Вставьте токен (НЕ пароль от GitHub!)

### Вариант 3: Сохранить в Git Credential Store

```bash
git config --global credential.helper store
```

При следующем `git push` введите:
- Username: ваш GitHub username
- Password: токен

Git сохранит эти данные для будущих операций.

---

## 📋 **ШАГ 7: Проверить настройку**

```bash
cd "D:\Java\Project RU VK-50\Projects Android\VkBookAndroid"
git remote -v
```

Должно показать URL с токеном (если использовали Вариант 1).

---

## 📋 **ШАГ 8: Выполнить push**

```bash
git push origin main
```

Если всё настроено правильно, push пройдет успешно!

---

## ⚠️ **ВАЖНЫЕ ЗАМЕЧАНИЯ**

### Безопасность:
- ❌ **НИКОГДА** не коммитьте токен в репозиторий
- ❌ **НИКОГДА** не делитесь токеном публично
- ✅ Храните токен в безопасном месте
- ✅ Используйте менеджер паролей для хранения токенов
- ✅ Регулярно обновляйте токены (если не используете "No expiration")

### Если токен утерян:
1. Перейдите в **Settings → Developer settings → Personal access tokens**
2. Найдите токен в списке
3. Нажмите **Revoke** (Отозвать) для удаления
4. Создайте новый токен по инструкции выше

### Если токен скомпрометирован:
1. **НЕМЕДЛЕННО** отзовите токен в настройках GitHub
2. Создайте новый токен
3. Обновите настройки Git с новым токеном

---

## 🔍 **Проверка текущих настроек**

Чтобы проверить, какой URL используется для push:

```bash
git remote -v
```

Чтобы проверить, сохранены ли учетные данные:

```bash
git config --global credential.helper
```

---

## 📞 **Если возникли проблемы**

### Ошибка: "Authentication failed"
- Проверьте, что токен скопирован полностью (без пробелов)
- Убедитесь, что токен не истек
- Проверьте, что токен имеет права `repo`

### Ошибка: "Permission denied"
- Убедитесь, что токен имеет права на запись в репозиторий
- Проверьте, что вы используете правильный username

### Ошибка: "Repository not found"
- Проверьте правильность имени репозитория в URL
- Убедитесь, что у вас есть доступ к репозиторию

---

**Дата создания:** 2025-12-15  
**Версия:** 1.0








