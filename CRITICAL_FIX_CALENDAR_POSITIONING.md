# 🐛 КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Позиционирование календаря относительно графика

## Дата: 22.10.2025

---

## 🎯 НАЙДЕННАЯ ПРОБЛЕМА

**Февраль и март 2026 года** выпадали из паттерна смен!

### Причина:

Календарь вычислялся **ОТНОСИТЕЛЬНО БАЗОВОГО ПАТТЕРНА (0-9)**, а НЕ относительно **СДВИНУТОГО ГРАФИКА**!

```
ОШИБОЧНАЯ ЛОГИКА:

1. График сдвигается на позицию januaryCalculatedShift (например, 6)
2. Календарь вычисляется: adjusted = calculated - yearShiftOffset
   - yearShiftOffset = januaryCalculatedShift - januaryAdjustedShift
   - Это вычитание в БАЗОВОМ паттерне (0-9)!
   
3. НО график - это 36-элементный массив!
4. РАССИНХРОНИЗАЦИЯ между календарем и графиком!
```

---

## ❌ ОШИБОЧНАЯ ЛОГИКА (ДО ИСПРАВЛЕНИЯ)

### Строка 239:

```kotlin
val adjustedShift = calculatedShift - yearShiftOffset
```

### Проблема:

```
2026 год:
  januaryCalculatedShift = 6 (в базовом паттерне 0-9)
  januaryAdjustedShift = 0
  yearShiftOffset = 6 - 0 = 6

ЯНВАРЬ:
  calculated = 6 (в базовом 0-9)
  adjusted = 6 - 6 = 0
  ✅ Правильно (случайно)

ФЕВРАЛЬ:
  calculated = 2 (в базовом 0-9)  ← Это позиция в БАЗОВОМ паттерне!
  adjusted = 2 - 6 = -4
  Нормализуется к 6
  
  НО! График сдвинут на 6 позиций в 36-ЭЛЕМЕНТНОМ массиве!
  Календарь думает, что февраль на позиции 6, но это НЕПРАВИЛЬНАЯ позиция!
  
  ПРАВИЛЬНАЯ позиция февраля в сдвинутом графике:
    В 36-элементном: февраль на позиции (2 + 31) = 33 от января
    Относительно сдвинутого графика: (33 - 6) % 36 = 27
    
  ❌ РАССИНХРОНИЗАЦИЯ!
```

---

## ✅ ПРАВИЛЬНАЯ ЛОГИКА (ПОСЛЕ ИСПРАВЛЕНИЯ)

### Строка 241:

```kotlin
val adjustedShift = (calculatedShift - januaryCalculatedShift + 360) % 36
```

### Объяснение:

1. **График** начинается с позиции `januaryCalculatedShift` в 36-элементном окне
2. **Месяц** находится на позиции `calculatedShift` в базовом паттерне
3. **Относительная позиция** месяца в сдвинутом графике:
   ```
   adjustedShift = (calculatedShift - januaryCalculatedShift) % 36
   ```
4. **+360** для избежания отрицательных чисел (360 = 36 * 10)

---

## 🧮 МАТЕМАТИКА ИСПРАВЛЕНИЯ

### 2025 год (без сдвига):

```
januaryCalculatedShift = 1

ЯНВАРЬ:
  calculated = 1
  adjusted = (1 - 1 + 360) % 36 = 0 ✅

ФЕВРАЛЬ:
  calculated = 2
  adjusted = (2 - 1 + 360) % 36 = 1 ✅

МАРТ:
  calculated = 0
  adjusted = (0 - 1 + 360) % 36 = 359 % 36 = 35
  
  СТОП! Март не может быть на позиции 35!
  35 + 31 = 66 > 36 ❌
  
  Сработает нормализация (сдвиг -10):
  adjusted = (35 - 10) % 36 = 25
  25 + 31 = 56 > 36 ❌
  
  Еще сдвиг:
  adjusted = (25 - 10) % 36 = 15
  15 + 31 = 46 > 36 ❌
  
  Еще:
  adjusted = (15 - 10) % 36 = 5
  5 + 31 = 36 ✅
```

### 2026 год (со сдвигом):

```
januaryCalculatedShift = 6

ЯНВАРЬ:
  calculated = 6
  adjusted = (6 - 6 + 360) % 36 = 0 ✅

ФЕВРАЛЬ:
  calculated = 2  (в базовом паттерне)
  
  НО! Нужна позиция в 36-элементном паттерне!
  Февраль начинается через 31 день после января:
  calculated_36 = (6 + 31) % 36 = 37 % 36 = 1
  
  adjusted = (1 - 6 + 360) % 36 = 355 % 36 = 31
  31 + 28 = 59 > 36 ❌
  
  Нормализация:
  adjusted = (31 - 10) % 36 = 21
  21 + 28 = 49 > 36 ❌
  
  adjusted = (21 - 10) % 36 = 11
  11 + 28 = 39 > 36 ❌
  
  adjusted = (11 - 10) % 36 = 1
  1 + 28 = 29 ✅
```

**СТОП!** Есть проблема! `calculateMonthShift` возвращает позицию в **БАЗОВОМ паттерне (0-9)**, а не в 36-элементном!

Нужно исправить!

---

## 🚨 ОБНАРУЖЕНА ЕЩЕ ОДНА ПРОБЛЕМА!

`calculateMonthShift` возвращает значение 0-9 (базовый паттерн), но нам нужна позиция в 36-элементном!

### Решение:

Нужно вычислять **количество дней от января**:

```kotlin
val daysFromJanuary = (0 until monthIndex).sum { daysInMonths[it] }
val adjustedShift = (januaryCalculatedShift + daysFromJanuary) % 36
```

НО! Это не учитывает, что график сдвинут!

### ПРАВИЛЬНОЕ решение:

1. Вычисляем позицию месяца в 36-элементном паттерне (от 1 января 2025)
2. Вычитаем позицию января текущего года
3. Получаем относительную позицию в окне

```kotlin
// Количество дней от 1 января текущего года до начала месяца
val daysFromJanuary = (0 until monthIndex).sum { daysInMonths[it] }

// Позиция месяца относительно сдвинутого графика
val adjustedShift = daysFromJanuary % 36
```

Это правильно, потому что график уже сдвинут на `januaryCalculatedShift`!

---

## ✅ ФИНАЛЬНОЕ ИСПРАВЛЕНИЕ

Изменю логику еще раз:

```kotlin
// Вычисляем количество дней от января текущего года
val daysFromJanuary = (0 until monthIndex).sum { daysInMonths[it] }

// Позиция месяца в окне (график уже сдвинут на januaryCalculatedShift)
val adjustedShift = daysFromJanuary % 36
```

Это НАМНОГО проще и правильнее!

---

## 📊 ПРОВЕРКА

### 2025 год:

```
ЯНВАРЬ:
  daysFromJanuary = 0
  adjusted = 0 % 36 = 0
  График начинается с позиции 1
  Календарь на позиции 0
  ... нет, это неправильно!
```

Подождите, график сдвигается циклически, но календарь позиционируется линейно!

Нужна другая логика...

---

## 💡 ПРАВИЛЬНОЕ ПОНИМАНИЕ

График сдвигается в **36-элементном цикле**.
Календарь позиционируется **последовательно** (январь → февраль → март).

**Ключ:** Календарь должен показывать дни месяца, а график должен показывать соответствующие смены!

**Простое решение:** Календарь всегда линейный (январь с пустыми ячейками слева), а график сдвигается циклически!

Вернемся к исходной идее с `calculatedShift - januaryCalculatedShift`, но правильно!






