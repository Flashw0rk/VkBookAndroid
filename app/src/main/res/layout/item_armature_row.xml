<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:background="?android:attr/selectableItemBackground"
    android:paddingStart="16dp"
    android:paddingTop="16dp"
    android:paddingEnd="16dp"
    android:paddingBottom="16dp"
    android:gravity="center_vertical">

    <ImageView
        android:id="@+id/itemIcon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="#e3f2fd"
        android:scaleType="centerInside"
        android:padding="8dp"
        android:layout_marginEnd="16dp"
        android:contentDescription="Иконка элемента"
        tools:src="@drawable/ic_armature_valve" /> <!-- Заглушка, реальные иконки по типу -->

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/itemTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:textSize="16sp"
            android:textColor="#212121"
            tools:text="Клапан паровой основной" />

        <TextView
            android:id="@+id/itemSubtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textColor="#757575"
            tools:text="Тип: Запорный • DN 400 • PN 130" />

        <TextView
            android:id="@+id/itemDescription"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="#9e9e9e"
            tools:text="Главный паровой клапан турбины • Схема: Л.123-45 • Поз. 15A" />

    </LinearLayout>

    <ImageView
        android:id="@+id/itemActionIcon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginStart="8dp"
        android:clickable="true"
        android:focusable="true"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Открыть документ"
        tools:src="@drawable/ic_pdf_action" /> <!-- Заглушка, реальная иконка действия -->

</LinearLayout> 