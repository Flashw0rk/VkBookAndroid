# 📡 Оптимизация для плохой сети (2G, медленная, частичная потеря пакетов)

## 🔍 Текущее состояние

### Таймауты:
- **Основные запросы:** 10-15 секунд
- **Wakeup ping:** 2 секунды
- **Проблема:** Слишком короткие для 2G и медленной сети

### Поведение при плохой сети:
- ✅ Проверка сети перед запросом
- ✅ Fire-and-forget для wakeup ping
- ⚠️ Фиксированные таймауты (не адаптивные)
- ⚠️ Нет retry для медленных соединений

## 🎯 Проблемы при плохой сети

### 1. **2G сеть (медленная)**
```
Скорость: 50-200 Кбит/с
Задержка: 300-1000 мс
Проблема: Таймаут 10-15 сек может быть недостаточно для больших файлов
```

### 2. **Частичная потеря пакетов**
```
Проблема: Запросы могут частично проходить, но терять пакеты
Результат: Таймауты могут срабатывать преждевременно
```

### 3. **Медленная сеть**
```
Проблема: Запросы проходят, но очень медленно
Результат: Таймауты могут срабатывать до завершения
```

## ✅ Решения

### 1. **Адаптивные таймауты в зависимости от типа сети**

Нужно добавить функцию определения типа сети и адаптивные таймауты:

```kotlin
// NetworkUtils.kt - добавить функцию определения типа сети
fun getNetworkType(context: Context): NetworkType {
    val connectivityManager = context.getSystemService(Context.CONNECTIVITY_SERVICE) as? ConnectivityManager
        ?: return NetworkType.UNKNOWN
    
    val network = connectivityManager.activeNetwork ?: return NetworkType.UNKNOWN
    val capabilities = connectivityManager.getNetworkCapabilities(network) ?: return NetworkType.UNKNOWN
    
    return when {
        capabilities.hasTransport(NetworkCapabilities.TRANSPORT_WIFI) -> NetworkType.WIFI
        capabilities.hasTransport(NetworkCapabilities.TRANSPORT_CELLULAR) -> {
            // Определяем поколение сети
            val downlinkKbps = capabilities.linkDownstreamBandwidthKbps
            when {
                downlinkKbps < 100 -> NetworkType.G2  // 2G
                downlinkKbps < 1000 -> NetworkType.G3  // 3G
                else -> NetworkType.G4  // 4G/5G
            }
        }
        else -> NetworkType.UNKNOWN
    }
}

enum class NetworkType {
    WIFI, G4, G3, G2, UNKNOWN
}

fun getAdaptiveTimeouts(networkType: NetworkType): Timeouts {
    return when (networkType) {
        NetworkType.WIFI -> Timeouts(10, 15, 10)  // Быстрая сеть
        NetworkType.G4 -> Timeouts(15, 30, 15)     // 4G
        NetworkType.G3 -> Timeouts(20, 45, 20)    // 3G
        NetworkType.G2 -> Timeouts(30, 60, 30)    // 2G - увеличенные таймауты
        NetworkType.UNKNOWN -> Timeouts(20, 30, 20)  // Безопасные значения
    }
}

data class Timeouts(
    val connect: Long,  // секунды
    val read: Long,     // секунды
    val write: Long     // секунды
)
```

### 2. **Увеличенные таймауты для wakeup ping при плохой сети**

```kotlin
// SyncService.wakeupServerPing()
val networkType = NetworkUtils.getNetworkType(context)
val timeouts = NetworkUtils.getAdaptiveTimeouts(networkType)

val pingClient = OkHttpClient.Builder()
    .connectTimeout(timeouts.connect, TimeUnit.SECONDS)
    .readTimeout(timeouts.read, TimeUnit.SECONDS)
    .writeTimeout(timeouts.write, TimeUnit.SECONDS)
    // ...
```

### 3. **Retry логика для медленных соединений**

```kotlin
suspend fun <T> retryOnSlowNetwork(
    maxAttempts: Int = 3,
    initialDelay: Long = 1000,
    maxDelay: Long = 5000,
    block: suspend () -> T
): T? {
    var attempt = 0
    var delay = initialDelay
    
    while (attempt < maxAttempts) {
        try {
            return block()
        } catch (e: SocketTimeoutException) {
            attempt++
            if (attempt >= maxAttempts) {
                Log.w("NetworkUtils", "Max retries reached for slow network")
                return null
            }
            Log.d("NetworkUtils", "Retry $attempt/$maxAttempts after timeout (delay: ${delay}ms)")
            delay(delay)
            delay = minOf(delay * 2, maxDelay)  // Exponential backoff
        } catch (e: IOException) {
            // Другие ошибки сети - не ретраим
            throw e
        }
    }
    return null
}
```

### 4. **Улучшенная обработка частичной потери пакетов**

```kotlin
// Добавить в NetworkModule
.addInterceptor { chain ->
    val request = chain.request()
    val response = try {
        chain.proceed(request)
    } catch (e: SocketTimeoutException) {
        // При таймауте проверяем, может быть частичная передача
        Log.w("NetworkModule", "Timeout, but request may have been sent")
        throw e
    }
    
    // Проверяем целостность ответа
    if (response.code == 200 && response.body != null) {
        val contentLength = response.header("Content-Length")?.toLongOrNull()
        val actualLength = response.body?.contentLength() ?: -1
        
        if (contentLength != null && actualLength > 0 && actualLength < contentLength) {
            Log.w("NetworkModule", "Partial response received: $actualLength/$contentLength bytes")
            // Можно попробовать retry или использовать частичные данные
        }
    }
    
    response
}
```

## 📊 Поведение при разных типах сети

### WiFi (быстрая сеть)
```
Таймауты: 10/15/10 сек
Wakeup ping: ✅ Работает быстро (< 1 сек)
Синхронизация: ✅ Работает быстро
Статистика сервера: ✅ Работает быстро
```

### 4G (хорошая мобильная сеть)
```
Таймауты: 15/30/15 сек
Wakeup ping: ✅ Работает (1-3 сек)
Синхронизация: ✅ Работает (зависит от размера файлов)
Статистика сервера: ✅ Работает
```

### 3G (средняя мобильная сеть)
```
Таймауты: 20/45/20 сек
Wakeup ping: ✅ Работает (2-5 сек)
Синхронизация: ⚠️ Медленно, но работает
Статистика сервера: ✅ Работает медленно
```

### 2G (медленная мобильная сеть)
```
Таймауты: 30/60/30 сек (увеличены)
Wakeup ping: ⚠️ Может не пройти (таймаут 2 сек слишком короткий)
Синхронизация: ⚠️ Очень медленно, может не пройти
Статистика сервера: ⚠️ Может не пройти
```

## 🔧 Рекомендации по улучшению

### 1. **Увеличить таймауты для wakeup ping при плохой сети**
- Текущий: 2 секунды
- Рекомендуемый: 10-30 секунд в зависимости от типа сети

### 2. **Добавить retry для wakeup ping**
- При таймауте повторить 1-2 раза
- С экспоненциальной задержкой

### 3. **Адаптивные таймауты**
- Определять тип сети
- Устанавливать таймауты в зависимости от типа

### 4. **Улучшить обработку ошибок**
- Различать таймауты от других ошибок
- Показывать понятные сообщения пользователю

## ⚠️ Текущие проблемы

1. **Wakeup ping при 2G:**
   - Таймаут 2 сек слишком короткий
   - Может не пройти при медленной сети

2. **Синхронизация при 2G:**
   - Таймаут 15 сек может быть недостаточно для больших файлов
   - Нет retry логики

3. **Статистика сервера при 2G:**
   - Таймаут 90 сек (ServerInfoRepository) - хорошо
   - Но может не пройти при очень медленной сети

## ✅ Что уже работает хорошо

1. **Проверка сети перед запросом** - экономит время
2. **Fire-and-forget для wakeup ping** - не блокирует
3. **Кэширование** - работает офлайн
4. **Обработка ошибок** - приложение не падает

## 🎯 Итоговые гарантии

### При плохой сети (2G, медленная):
1. ✅ **Wakeup ping:** Может не пройти (таймаут 2 сек), но не блокирует приложение
2. ⚠️ **Синхронизация:** Может не пройти (таймаут 15 сек), но приложение работает офлайн
3. ⚠️ **Статистика сервера:** Может не пройти, но не критично
4. ✅ **Приложение работает:** Все функции доступны офлайн

### При частичной потере пакетов:
1. ⚠️ **Могут быть таймауты:** Но приложение не падает
2. ✅ **Retry логика:** Помогает при временных проблемах
3. ✅ **Обработка ошибок:** Понятные сообщения пользователю































