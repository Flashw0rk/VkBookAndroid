# 📘 Полная инструкция: Коммиты и Push на GitHub

## 🔧 **НАСТРОЙКА GIT (выполнить один раз)**

### 1. Настройка пользователя
```bash
git config --global user.name "ВашеИмя"
git config --global user.email "ваш@email.com"
```

### 2. Настройка для работы с русскими символами (КРИТИЧНО!)
```bash
# Отключаем квотирование путей (для правильного отображения русских символов)
git config --global core.quotepath false

# Устанавливаем UTF-8 для коммитов
git config --global i18n.commitencoding utf-8

# Устанавливаем UTF-8 для логов
git config --global i18n.logoutputencoding utf-8
```

### 3. Настройка удаленного репозитория
```bash
# Если репозиторий еще не добавлен:
git remote add origin https://github.com/Flashw0rk/VkBookAndroid.git

# Если нужно изменить URL (с токеном):
git remote set-url origin https://ghp_ВАШ_ТОКЕН@github.com/Flashw0rk/VkBookAndroid.git
```

### 4. Получение Personal Access Token (PAT) на GitHub
1. Зайти на GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выбрать права: `repo` (полный доступ к репозиториям)
4. Скопировать токен (показывается только один раз!)
5. Использовать в URL: `https://ghp_ВАШ_ТОКЕН@github.com/Flashw0rk/VkBookAndroid.git`

---

## 📝 **ПРОЦЕСС КОММИТА И PUSH (каждый раз)**

### Шаг 1: Проверка статуса
```bash
cd "D:\Java\Project RU VK-50\Projects Android\VkBookAndroid"
git status
```

### Шаг 2: Добавление измененных файлов
```bash
# Добавить все измененные файлы
git add .

# ИЛИ добавить конкретные файлы
git add app/src/main/java/com/example/vkbookandroid/MainActivity.kt
git add app/src/main/res/layout/fragment_armature.xml
```

### Шаг 3: Создание коммита с русским сообщением

#### ⚠️ ВАЖНО: В Windows PowerShell используем один из методов:

#### **Метод 1: Прямой коммит (рекомендуется)**
```bash
# Устанавливаем кодировку для текущей сессии PowerShell
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$env:PYTHONIOENCODING = "utf-8"

# Коммит с русским сообщением
git commit -m "Ваше русское сообщение коммита"
```

#### **Метод 2: Через временный файл (надежнее для сложных сообщений)**
```bash
# Создаем файл с сообщением в UTF-8
@"
Ваше русское сообщение коммита
Может быть многострочным
"@ | Out-File -FilePath commit_msg.txt -Encoding UTF8

# Коммит из файла
git commit -F commit_msg.txt

# Удаляем временный файл
del commit_msg.txt
```

#### **Метод 3: Через chcp (смена кодовой страницы)**
```bash
# Меняем кодовую страницу на UTF-8
chcp 65001

# Коммит
git commit -m "Ваше русское сообщение коммита"
```

### Шаг 4: Проверка коммита
```bash
# Просмотр последних коммитов
git log --oneline -5

# Просмотр с русскими символами
git log --oneline -5 --format="%h %s"
```

### Шаг 5: Push на GitHub
```bash
# Push в основную ветку
git push origin main

# ИЛИ если ветка называется master
git push origin master

# ИЛИ push с установкой upstream (первый раз)
git push -u origin main
```

---

## 🎯 **ПОЛНЫЙ ПРИМЕР СЕССИИ**

```bash
# 1. Переход в директорию проекта
cd "D:\Java\Project RU VK-50\Projects Android\VkBookAndroid"

# 2. Проверка статуса
git status

# 3. Добавление файлов
git add .

# 4. Настройка кодировки для PowerShell (если нужно)
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8

# 5. Коммит
git commit -m "Добавлен индикатор загрузки при поиске в вкладке Арматура"

# 6. Проверка коммита
git log --oneline -1

# 7. Push
git push origin main
```

---

## 🔍 **ПРОВЕРКА НАСТРОЕК**

### Проверить текущие настройки:
```bash
git config --list | findstr /i "user.name user.email core.quotepath i18n"
```

### Должно быть:
```
user.name=ВашеИмя
user.email=ваш@email.com
core.quotepath=false
i18n.commitencoding=utf-8
i18n.logoutputencoding=utf-8
```

---

## ⚠️ **РЕШЕНИЕ ПРОБЛЕМ**

### Проблема: "NUL byte in commit log message"
**Решение:** Использовать метод 2 (через временный файл) или метод 3 (chcp 65001)

### Проблема: "Authentication failed"
**Решение:** 
1. Проверить токен в `git remote -v`
2. Создать новый PAT на GitHub
3. Обновить URL: `git remote set-url origin https://ghp_НОВЫЙ_ТОКЕН@github.com/Flashw0rk/VkBookAndroid.git`

### Проблема: Русские символы отображаются как "С‚РµСЃС‚"
**Решение:**
```bash
git config --global core.quotepath false
git config --global i18n.commitencoding utf-8
git config --global i18n.logoutputencoding utf-8
```

### Проблема: "paths did not match any file(s) known to git"
**Решение:** Убедиться, что файлы добавлены через `git add .` или `git add путь/к/файлу`

---

## 📋 **ЧЕКЛИСТ ПЕРЕД КОММИТОМ**

- [ ] Проверить статус: `git status`
- [ ] Убедиться, что изменены только нужные файлы
- [ ] Добавить файлы: `git add .`
- [ ] Настроить кодировку PowerShell (если нужно)
- [ ] Создать коммит с понятным сообщением
- [ ] Проверить коммит: `git log --oneline -1`
- [ ] Выполнить push: `git push origin main`

---

## 🔐 **БЕЗОПАСНОСТЬ**

⚠️ **ВАЖНО:** Токен в URL виден в `git remote -v`. Для безопасности:
1. Используйте SSH вместо HTTPS (рекомендуется)
2. Или используйте Git Credential Manager
3. Или храните токен в переменной окружения

### Настройка SSH (альтернатива):
```bash
# Генерация SSH ключа
ssh-keygen -t ed25519 -C "ваш@email.com"

# Добавление ключа в ssh-agent
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519

# Копировать публичный ключ и добавить на GitHub
cat ~/.ssh/id_ed25519.pub

# Изменить remote на SSH
git remote set-url origin git@github.com:Flashw0rk/VkBookAndroid.git
```

---

## 📚 **ДОПОЛНИТЕЛЬНЫЕ КОМАНДЫ**

### Просмотр изменений перед коммитом:
```bash
git diff
```

### Отмена добавления файла:
```bash
git reset HEAD путь/к/файлу
```

### Изменение последнего коммита:
```bash
git commit --amend -m "Новое сообщение"
```

### Просмотр удаленных репозиториев:
```bash
git remote -v
```

### Просмотр всех веток:
```bash
git branch -a
```

---

**Дата создания:** 2025-12-15  
**Версия:** 1.0











