# 📱 ИНСТРУКЦИЯ: УСТАНОВКА И ТЕСТИРОВАНИЕ ИСПРАВЛЕННОГО APK

## 🎯 Что было исправлено?

**КРИТИЧЕСКАЯ ПРОБЛЕМА:** FileObserver не был активирован, из-за чего вкладка "График проверок" НЕ обновлялась автоматически после синхронизации с сервером.

**РЕШЕНИЕ:** Добавлен вызов `setupFileWatching()` в `MainActivity.onCreate()`, теперь приложение автоматически отслеживает изменения файла "График проверок.xlsx" и обновляет данные.

---

## 📦 ШАГИ УСТАНОВКИ

### Шаг 1: Найдите новый APK
```
Путь: D:\Java\Projects Android\VkBookAndroid\app\build\outputs\apk\debug\app-debug.apk
```

### Шаг 2: Удалите старое приложение
```
1. На устройстве/эмуляторе откройте "Настройки" → "Приложения"
2. Найдите "VkBookAndroid"
3. Нажмите "Удалить"
4. Подтвердите удаление
```

### Шаг 3: Установите новый APK
```
1. Скопируйте app-debug.apk на устройство
2. Откройте файл через файловый менеджер
3. Разрешите установку из неизвестных источников (если требуется)
4. Нажмите "Установить"
```

---

## 🧪 ТЕСТИРОВАНИЕ

### Подготовка Logcat

**ВАЖНО:** Используйте специальный фильтр для просмотра нужных логов:

```
Фильтр: ChecksSchedule|MainActivity|UpdatesFileService
```

**Как настроить фильтр в Android Studio:**
1. Откройте вкладку "Logcat"
2. В поле поиска введите: `ChecksSchedule|MainActivity|UpdatesFileService`
3. Очистите логи (кнопка 🗑️)

---

### ТЕСТ #1: Первоначальная загрузка

**Что делать:**
```
1. Очистите логи (🗑️)
2. Запустите приложение
3. Откройте вкладку "График проверок"
```

**Что должно появиться в логах:**
```
✅ MainActivity D Setting up file watching for automatic data refresh
✅ MainActivity D Starting to watch file: .../График проверок.xlsx
✅ ChecksSchedule D === LOAD DATA CALLED ===
✅ ChecksSchedule D === ДИАГНОСТИКА ЗАГРУЗКИ ГРАФИКА ПРОВЕРОК ===
✅ ChecksSchedule D Excel path: .../files/data/График проверок.xlsx
✅ ChecksSchedule D Excel exists: true/false
✅ ChecksSchedule D Files in data dir: ...
```

**Если Excel файл существует:**
```
✅ ChecksSchedule D Loading from Excel: ...
✅ ChecksSchedule D Parser returned N items
✅ ChecksSchedule D   Item: [название операции] with N rules
✅ ChecksSchedule D ✅ Successfully loaded N items from Excel
✅ ChecksSchedule D Loaded N items total
✅ ChecksSchedule D === LOAD DATA COMPLETE ===
```

**Если Excel файл НЕ существует:**
```
⚠ ChecksSchedule W ⚠ Excel file not found, falling back to CSV
✅ ChecksSchedule D Loading from CSV resources
✅ ChecksSchedule D CSV loaded 5 items
```

---

### ТЕСТ #2: Синхронизация с сервером

**Что делать:**
```
1. Очистите логи (🗑️)
2. В приложении нажмите кнопку "Синхронизация"
3. Дождитесь завершения (статус "Обновление завершено")
4. НЕ переходите во вкладку "График проверок" вручную
```

**Что должно появиться в логах:**
```
✅ UpdatesFileService D === DOWNLOADING FILE: График проверок.xlsx ===
✅ UpdatesFileService D Target directory: .../files/data
✅ UpdatesFileService D Target file: .../files/data/График проверок.xlsx
✅ UpdatesFileService D ✅ File saved: ... (NNNN bytes)
✅ UpdatesFileService D 🎯 ГРАФИК ПРОВЕРОК successfully downloaded!
✅ UpdatesFileService D    Path: ...
✅ UpdatesFileService D    Size: NNNN bytes
✅ UpdatesFileService D    Exists: true
✅ UpdatesFileService D    Readable: true
```

**Затем АВТОМАТИЧЕСКИ должно появиться:**
```
✅ MainActivity D График проверок.xlsx changed, refreshing ChecksScheduleFragment
✅ ChecksSchedule D === REFRESH DATA CALLED ===
✅ ChecksSchedule D Reloading data from Excel/CSV...
✅ ChecksSchedule D === LOAD DATA CALLED ===
✅ ChecksSchedule D === ДИАГНОСТИКА ЗАГРУЗКИ ГРАФИКА ПРОВЕРОК ===
... (повторяются логи загрузки)
✅ ChecksSchedule D === REFRESH DATA COMPLETE ===
```

---

### ТЕСТ #3: Проверка отображения данных

**Что делать:**
```
1. Откройте вкладку "График проверок"
2. Проверьте что данные отображаются
3. Проверьте колонку "Повторения"
```

**Что должно быть:**
```
✅ Названия операций из Excel файла
✅ Правила напоминаний в колонке "Повторения"
✅ Правила отформатированы читабельно:
   - "Каждый день с 08:00 до 14:00"
   - "Каждый 1-й понедельник в 02:00"
   - "По: ПН, СР, ПТ в 10:00, 14:00"
```

---

## 📊 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

### ✅ Успешный результат:

1. **FileObserver активирован:**
   - В логах есть: `MainActivity D Setting up file watching`
   - В логах есть: `MainActivity D Starting to watch file`

2. **Данные загружаются при первом открытии:**
   - В логах есть: `ChecksSchedule D === LOAD DATA CALLED ===`
   - В логах есть: `ChecksSchedule D === ДИАГНОСТИКА ЗАГРУЗКИ ===`

3. **Синхронизация работает:**
   - Файл скачивается: `UpdatesFileService D 🎯 ГРАФИК ПРОВЕРОК successfully downloaded!`
   - Вкладка обновляется АВТОМАТИЧЕСКИ: `ChecksSchedule D === REFRESH DATA CALLED ===`

4. **Данные отображаются:**
   - Операции из Excel файла видны в таблице
   - Правила напоминаний отформатированы корректно

---

## ❌ Возможные проблемы

### Проблема: "Excel file not found"

**Причина:** Файл "График проверок.xlsx" не существует на сервере или не был скачан.

**Решение:**
1. Проверьте что файл есть на сервере в папке `/opt/vkbook-server/updates/`
2. Выполните синхронизацию вручную
3. Проверьте логи `UpdatesFileService` на наличие ошибок

---

### Проблема: "Parser returned 0 items"

**Причина:** Excel файл существует, но пустой или имеет неправильный формат.

**Решение:**
1. Откройте файл "График проверок.xlsx" в Excel
2. Проверьте что:
   - Столбец A: Операция (название задачи)
   - Столбец B: Частота (ЕЖЕДНЕВНО, 1 и 3 понедельник, и т.д.)
   - Столбец C: Время (08:00-14:00, 02:00; 06:00; 10:00, и т.д.)
3. Первая строка должна быть заголовками
4. Данные начинаются со 2-й строки

---

### Проблема: Автообновление не работает

**Симптомы:**
- Файл скачивается успешно
- НО в логах НЕТ: `MainActivity D График проверок.xlsx changed`
- НЕТ: `ChecksSchedule D === REFRESH DATA CALLED ===`

**Диагностика:**
1. Проверьте логи при запуске приложения
2. Должно быть: `MainActivity D Setting up file watching`
3. Если НЕТ этих логов - APK не был установлен правильно

**Решение:**
1. ПОЛНОСТЬЮ удалите старое приложение
2. Установите новый APK
3. Перезапустите устройство/эмулятор
4. Повторите тесты

---

## 📤 ОТПРАВКА ЛОГОВ

**Если что-то не работает**, пришлите мне логи в следующем формате:

### Формат логов:

```
=== ТЕСТ #1: ПЕРВОНАЧАЛЬНАЯ ЗАГРУЗКА ===
[вставьте логи здесь]

=== ТЕСТ #2: СИНХРОНИЗАЦИЯ ===
[вставьте логи здесь]

=== ТЕСТ #3: ОТОБРАЖЕНИЕ ДАННЫХ ===
[скриншот вкладки "График проверок"]
```

### Как собрать логи:

1. **В Android Studio:**
   - Откройте Logcat
   - Установите фильтр: `ChecksSchedule|MainActivity|UpdatesFileService`
   - Выделите все логи (Ctrl+A)
   - Скопируйте (Ctrl+C)

2. **Через ADB:**
   ```bash
   adb logcat -d | findstr /C:"ChecksSchedule" /C:"MainActivity" /C:"UpdatesFileService" > logs.txt
   ```

---

## ✅ ПОДТВЕРЖДЕНИЕ УСПЕХА

**Тест считается УСПЕШНЫМ, если:**

✅ В логах есть: `MainActivity D Setting up file watching`  
✅ В логах есть: `ChecksSchedule D === LOAD DATA CALLED ===`  
✅ После синхронизации появляется: `ChecksSchedule D === REFRESH DATA CALLED ===`  
✅ Данные из Excel отображаются в таблице  
✅ Правила напоминаний читабельны  

---

## 📞 Поддержка

Если возникли проблемы:
1. Соберите логи (см. "Отправка логов")
2. Сделайте скриншот вкладки "График проверок"
3. Отправьте мне с описанием проблемы

---

*Последнее обновление: 05.11.2025*  
*Версия APK: app-debug.apk (с исправлением FileObserver)*



















