[33m2312831[m[33m ([m[1;36mHEAD[m[33m -> [m[1;32mmain[m[33m)[m Добавил отображение подписи арматуры над выделением в MarkerOverlayView.kt (многострочный текст, ограничение ширины ~40мм, цвет как у маркера, без тени). Сделал «фиксированное» позиционирование подписи: всегда строго над прямоугольником и не двигается при зуме/скролле (убрал кламп к краям). Поддержал pinned-режим: добавил setMarkerInPageUnitsWithViewport(...), чтобы прямоугольник и подпись совпадали с тайлом высокого качества. Пробросил label и цвет из PdfViewerActivity.kt (парсинг цвета из marker_type #RRGGBB), показываю комментарии во всех режимах. Обновил ZoomableImageView.kt: при дабл-тапе триггерю onMatrixChanged, чтобы подпись и маркер сразу корректно перепозиционировались.
[33mac68d0f[m Добавил: Кэш Excel: контроль целостности (headers/widths/pages), авто‑пересборка при недостающих файлах, безопасный фолбэк без падений, исправлен ENOENT для widths.json. Поиск: единая логика фильтрации показывает только строки с вхождениями (ignoreCase) и подсвечивает совпавшие ячейки; улучшены цвета текста/подсказки. Синхронизация поиска: введён общий запрос между вкладками — текст и фильтр переносятся при переключении. UI/UX: удалена верхняя строка поиска; полностью отключены звуковые эффекты; у TabLayout добавлен padding по WindowInsets и задана минимальная высота. «Арматура»: подсветка и кликабельность в колонке «Арматура» только если в строке есть валидная ссылка на PDF в «PDF_Схема_и_ID_арматуры».
[33mc7ac52c[m Настроил вкладку схемы: Вынесена первичная загрузка PDF в корутины: копирование в cache, PdfRenderer, создание Bitmap и page.render(...) выполняются на Dispatchers.IO; обновления UI — на Main. Убраны configChanges у PdfViewerActivity: система пересоздаёт активити и автоматически применяет layout-land в альбомной ориентации. Кнопка «макс. качество»: Пересчёт видимости после layout и после установки битмапа; отложенный пересчёт при нулевых размерах. Пороги смягчены: учитываются и проценты, и dp (для больших схем ≥10% или ≥24dp; для остальных ≥14% или ≥32dp). Скрывается на малых схемах, при закреплённом вьюпорте и если прирост после клампа незначителен. Макс‑качество без смены масштаба: сохраняются текущий zoom и центр (setScaleAndCenter). Новый режим рендера видимой области: «standalone tile» в размер ImageView (RENDER_MODE_FOR_PRINT), без повторного рескейла — убраны смещения и размытость. Добавлена кнопка «вернуть полную схему»: выход из режима высококачественного вьюпорта, повторный рендер полной страницы базового качества и восстановление логики показа кнопки макс‑рендера. Жёсткие лимиты на размеры Bitmap (clamp): GL_MAX_TEXTURE_SIZE, Canvas‑safe cap (~96MB), до 60% доступной памяти heap; несколько попыток с понижением масштаба — предотвращает OOM/Canvas crash. Авто‑перерисовка по зуму: дебаунс 600мс + отложенный запуск 300мс; срабатывает только при существенном приросте и не для малых схем. Стартовый рендер малых схем ограничен до 2× экрана для устранения стартовых лагов и перерасхода памяти. Результат: быстрее старт, меньше лагов/крашей на слабых устройствах, стабильное появление кнопки там, где есть реальный прирост качества, детальный и чёткий рендер видимой области, удобный возврат к полной схеме.
[33m40d95a8[m Добавил возможность открытия пдф файлов
[33me38d968[m Налажена работа таблицы и редактирования размеров ячеек во вкладках "Сигналы БЩУ" и "Арматура" (таблица отображается корректно, столбцы выровнены, добавлена кнопка редактирования размеров столбцов с последующим сохранением этих размеров).
[33m13ac6ee[m Промежуточная настройка таблицы во вкладке "Сигналы БЩУ" (корректно отображается таблица, все ячейки расположены ровно, без сдвигов, таблица корректно прокручивается).
[33meea5acf[m Проект запускается на адройд
[33m10dcec2[m Тех коммит
[33m787db9b[m Initial commit
[33m1a35586[m Создал новый проект. Пробую реализовать функционал проекта VK-book-Java на мобильном телефоне с системой Android
