# 🔋 Оптимизация периодических wakeup ping запросов

## ✅ Реализовано

### 1. **Периодический wakeup ping каждые 10 минут**

**Реализация:**
```kotlin
// MainActivity.kt
private var periodicWakeupJob: Job? = null
private val PERIODIC_WAKEUP_INTERVAL_MS = 10 * 60 * 1000L // 10 минут

private fun startPeriodicWakeupPing() {
    periodicWakeupJob = uiScope.launch {
        while (isActive) {
            // Проверка сети перед запросом
            if (NetworkUtils.isNetworkAvailable(this@MainActivity)) {
                syncService.wakeupServerPing(force = false)  // Использует кэширование
            }
            delay(PERIODIC_WAKEUP_INTERVAL_MS)  // 10 минут
        }
    }
}
```

**Жизненный цикл:**
- ✅ Запускается в `onResume()` - когда приложение активно
- ✅ Останавливается в `onPause()` - экономия батареи в фоне
- ✅ Останавливается в `onDestroy()` - очистка ресурсов

### 2. **Оптимизация для минимальной нагрузки**

**Ключевые оптимизации:**

#### A. **Fire-and-forget (не ждем ответа)**
```kotlin
// SyncService.wakeupServerPing()
pingClient.newCall(request).enqueue(object : Callback {
    override fun onFailure(call: Call, e: IOException) {
        // Игнорируем ошибки - это нормально
    }
    override fun onResponse(call: Call, response: Response) {
        response.close()  // Закрываем и забываем
    }
})
```
**Результат:** Запрос отправляется и забывается, не блокирует выполнение.

#### B. **Короткие таймауты (2 секунды)**
```kotlin
val pingClient = OkHttpClient.Builder()
    .connectTimeout(2, TimeUnit.SECONDS)
    .readTimeout(2, TimeUnit.SECONDS)
    .writeTimeout(2, TimeUnit.SECONDS)
    .build()
```
**Результат:** Даже без сети запрос быстро завершится по таймауту.

#### C. **Проверка сети перед запросом**
```kotlin
// Быстрая проверка (< 1ms), не блокирует
if (!NetworkUtils.isNetworkAvailable(context)) {
    return  // Быстрый выход, не делаем запрос
}
```
**Результат:** Если сети нет, запрос не отправляется вообще.

#### D. **Кэширование (не чаще 1 раза в 5 минут)**
```kotlin
// SyncService.kt
private val WAKEUP_PING_INTERVAL_MS = 5 * 60 * 1000L // 5 минут

if (!force && now - lastWakeupPingTime < WAKEUP_PING_INTERVAL_MS) {
    return  // Пропускаем, если недавно уже отправляли
}
```
**Результат:** Даже если периодический запрос каждые 10 минут, реально отправляется не чаще 1 раза в 5 минут.

#### E. **Асинхронное выполнение**
```kotlin
// Все запросы в фоне, не блокируют UI
CoroutineScope(Dispatchers.IO).launch {
    // Сетевой запрос
}
```
**Результат:** Не блокирует главный поток и UI.

### 3. **Точки отправки wakeup ping**

#### A. **При запуске приложения**
```kotlin
// MainActivity.onCreate()
uiScope.launch(Dispatchers.IO) {
    syncService.wakeupServerPing(force = true)  // force = true пропускает кэш
}
```
**Когда:** При каждом запуске приложения
**Особенность:** `force = true` - отправляется всегда, даже если недавно отправляли

#### B. **При нажатии "Обновить базы"**
```kotlin
// MainActivity.waitForServerWakeup()
syncService.wakeupServerPing(force = true)  // Перед началом ожидания
// ...
syncService.wakeupServerPing(force = true)  // Перед каждой попыткой
```
**Когда:** При нажатии кнопки "Обновить базы"
**Особенность:** `force = true` - отправляется всегда для гарантированного пробуждения

#### C. **Периодически каждые 10 минут**
```kotlin
// MainActivity.startPeriodicWakeupPing()
syncService.wakeupServerPing(force = false)  // Использует кэширование
```
**Когда:** Каждые 10 минут пока приложение активно
**Особенность:** `force = false` - использует кэширование (не чаще 1 раза в 5 минут)

## 📊 Нагрузка на устройство

### Минимальная нагрузка благодаря оптимизациям:

1. **CPU:** Минимальная
   - Проверка сети: < 1ms
   - Отправка запроса: асинхронно в фоне
   - Fire-and-forget: не ждем ответа

2. **Батарея:** Минимальная
   - Короткие таймауты (2 сек) - быстро завершается
   - Проверка сети перед запросом - не тратим батарею без сети
   - Останавливается в `onPause()` - не работает в фоне

3. **Сеть:** Минимальная
   - Кэширование (не чаще 1 раза в 5 минут)
   - Fire-and-forget - не держим соединение
   - Короткие таймауты - быстро закрываем соединение

4. **Память:** Минимальная
   - Один Job для периодических запросов
   - Короткоживущие объекты (запросы быстро завершаются)

## 🎯 Итоговые гарантии

1. ✅ **Wakeup ping при запуске** - fire-and-forget, не блокирует
2. ✅ **Wakeup ping при "Обновить базы"** - fire-and-forget, не блокирует
3. ✅ **Wakeup ping каждые 10 минут** - оптимизирован, минимальная нагрузка
4. ✅ **Не нагружает устройство** - короткие таймауты, проверка сети, кэширование
5. ✅ **Не тормозит программу** - все асинхронно, fire-and-forget
6. ✅ **Экономия батареи** - останавливается в фоне (`onPause()`)

## 📝 Технические детали

### Схема работы периодического wakeup ping:

```
onResume() {
    startPeriodicWakeupPing()  // Запускаем периодический запрос
}

startPeriodicWakeupPing() {
    uiScope.launch {
        while (isActive) {
            // 1. Быстрая проверка сети (< 1ms)
            if (NetworkUtils.isNetworkAvailable()) {
                // 2. Асинхронный запрос (fire-and-forget)
                syncService.wakeupServerPing(force = false)
            }
            // 3. Ждем 10 минут
            delay(10 минут)
        }
    }
}

onPause() {
    stopPeriodicWakeupPing()  // Останавливаем для экономии батареи
}
```

### Кэширование предотвращает дубликаты:

```
Периодический запрос: каждые 10 минут
Кэширование SyncService: не чаще 1 раза в 5 минут

Результат:
- 0 мин: запрос отправлен ✅
- 5 мин: запрос отправлен ✅
- 10 мин: запрос отправлен ✅
- 15 мин: запрос отправлен ✅
- 20 мин: запрос отправлен ✅

Без кэширования было бы:
- 0 мин: запрос ✅
- 10 мин: запрос ✅
- 20 мин: запрос ✅

С кэшированием реально:
- 0 мин: запрос ✅
- 5 мин: запрос ✅ (вместо 10 мин)
- 10 мин: пропущен (кэш)
- 15 мин: запрос ✅
- 20 мин: пропущен (кэш)

Итого: больше запросов, но все равно не чаще 1 раза в 5 минут
```

## 🔧 Рекомендации

1. **Всегда используйте `force = false` для периодических запросов**
   - Позволяет кэшированию работать
   - Предотвращает избыточные запросы

2. **Используйте `force = true` только для критичных случаев**
   - При запуске приложения
   - При нажатии "Обновить базы"
   - Когда нужно гарантированно разбудить сервер

3. **Останавливайте периодические запросы в `onPause()`**
   - Экономия батареи
   - Не тратим ресурсы в фоне

4. **Всегда проверяйте сеть перед запросом**
   - Быстрая проверка (< 1ms)
   - Не тратим ресурсы без сети








