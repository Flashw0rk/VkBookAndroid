# 🔄 Руководство по интеграции Android с VkBookServer

## ✅ Что уже реализовано

### 1. **Сетевой слой**
- ✅ `NetworkModule` настроен на порт 8082
- ✅ `ArmatureApiService` с реальными эндпоинтами сервера
- ✅ `ArmatureRepository` для работы с данными
- ✅ `SyncService` для синхронизации

### 2. **UI интеграция**
- ✅ Кнопка синхронизации в `MainActivity`
- ✅ Статус синхронизации
- ✅ Автоматическая проверка соединения при запуске
- ✅ Методы `refreshData()` во всех фрагментах

### 3. **API эндпоинты**
- ✅ `GET /api/health` - проверка состояния
- ✅ `GET /api/files/pdf` - список PDF файлов
- ✅ `GET /api/files/json` - список JSON файлов
- ✅ `GET /api/files/excel` - список Excel файлов
- ✅ `GET /api/files/json/armature_coords.json` - скачивание координат
- ✅ `GET /api/files/excel/{filename}` - скачивание Excel
- ✅ `GET /api/files/pdf/{filename}` - скачивание PDF

## 🚀 Как протестировать

### 1. **Запуск сервера**
```bash
cd D:\Java\VkBookServer
./gradlew bootRun
```
Сервер должен запуститься на `http://localhost:8082`

### 2. **Запуск Android приложения**
```bash
cd "D:\Java\Projects Android\VkBookAndroid"
./gradlew assembleDebug
```

### 3. **Тестирование в эмуляторе**
- Установите APK на эмулятор
- Откройте приложение
- Нажмите кнопку "🔄 Синхронизация"
- Проверьте статус в верхней панели

### 4. **Тестирование на реальном устройстве**
- Измените URL в `NetworkModule.kt`:
```kotlin
private const val BASE_URL = "http://192.168.1.100:8082/" // IP вашего ПК
```
- Убедитесь, что устройство и ПК в одной сети

## 📱 Функциональность

### **Автоматическая проверка соединения**
- При запуске приложения проверяется доступность сервера
- Кнопка синхронизации активируется только при доступном сервере

### **Синхронизация данных**
- **armature_coords.json** - координаты маркеров
- **Excel файлы** - таблицы данных (Armatures.xlsx, Oborudovanie_BSCHU.xlsx)
- **PDF файлы** - схемы

### **Обновление UI**
- После синхронизации автоматически обновляются все фрагменты
- Данные загружаются заново из синхронизированных файлов

## 🔧 Настройка

### **Изменение URL сервера**
В файле `app/src/main/java/com/example/vkbookandroid/network/NetworkModule.kt`:
```kotlin
// Для эмулятора
private const val BASE_URL = "http://10.0.2.2:8082/"

// Для реального устройства (замените на IP вашего ПК)
private const val BASE_URL = "http://192.168.1.100:8082/"
```

### **Логирование**
Все операции синхронизации логируются с тегом `SyncService`:
```kotlin
Log.d("SyncService", "Starting sync...")
```

## 🐛 Отладка

### **Проверка соединения**
```bash
# Проверка доступности сервера
curl http://localhost:8082/api/health

# Проверка файлов
curl http://localhost:8082/api/files/excel
curl http://localhost:8082/api/files/json
```

### **Логи Android**
```bash
adb logcat | grep -E "(SyncService|ArmatureRepository|NetworkModule)"
```

## 📋 Следующие шаги

1. **Запустить сервер** VkBookServer
2. **Протестировать** синхронизацию в эмуляторе
3. **Настроить** IP для реального устройства
4. **Проверить** загрузку и отображение данных

## ⚠️ Важные замечания

- **Не нарушена** существующая функциональность приложения
- **Добавлена** только синхронизация с сервером
- **Сохранена** обратная совместимость с локальными файлами
- **Работает** как с сервером, так и без него

---

**Готово к тестированию!** 🎉


